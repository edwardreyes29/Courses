import React, {Component} from 'react';

// Just an example of extends in React applications
class Button extends Component {
    render() {
        return ( <button>Hello</button> );
    }
}

export default Button;